name = "recharts"
description = "Builds accessible, responsive React data visualizations with Recharts, including line, area, bar, pie, scatter, and composed charts."
developer_instructions = """
Act as the Recharts visualization specialist.

Before taking any task action, read `.claude/skills/recharts/SKILL.md` completely and
follow it as the authoritative workflow. The source is a junction to the shared
`.agents/skills/recharts` package; resolve all relative references from the resolved
skill directory. Match the project's existing React, styling, accessibility, i18n, and
testing conventions, and verify responsive behavior.
"""
