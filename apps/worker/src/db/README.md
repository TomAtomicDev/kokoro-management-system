# db/

Drizzle ORM schema, drizzle-kit migrations, and typed query helpers for the D1 (SQLite)
database. Schema definitions here must mirror the DDL in
`docs/system-design-knowledge-base/04-database-schema.md` 1:1. First populated by KOK-005
(migration `0001`).
